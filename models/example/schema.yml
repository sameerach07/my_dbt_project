
version: 2

sources:
  - name: raw
    schema: public
    tables:
      - name: provet_clients_new
        description: provet Clients Staging table
        freshness:
          warn_after: { count: 24, period: hour }
          error_after: { count: 48, period: hour }

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique                

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique                
    - name: ConnectRawdata
      description: "Its a raw source from Snowflake"
      columns:
        - name: client_id
          description: "Its client id column"
          tests:
            - unique
            - not_null
            
          
